# =============================================================================
# 通用型組織績效評核偏誤自動化偵測框架 — Python 套件需求
# 安裝：pip install -r requirements.txt
# 註：版本為相容性下限；論文最終定稿時，建議在你的環境執行
#     `pip freeze > requirements.txt` 鎖定確切版本，以利完全重現。
# =============================================================================

streamlit>=1.30
pandas>=2.0
numpy>=1.26,<2.0          # 若 shap/numba 報相容性錯誤，請維持 <2.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
scipy>=1.11
shap>=0.44               # SHAP 可解釋性分析
semopy>=2.3              # 結構方程模型 (SEM) 潛在變數萃取
